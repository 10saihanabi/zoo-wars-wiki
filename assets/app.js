(()=>{const D=window.ZOO_WIKI;D.rules=D.rules.map(r=>r[0]==='コスト自然回復速度'?['コスト自然回復速度','2秒に1コスト','game','ゲーム内確認']:r);D.wanted=D.wanted.filter(x=>!x.includes('コスト自然回復速度'));const pollOptions=[['ブドウ',-314,-240],['トラ',-314,-125],['スイカ',-415,-240],['モグラ',-314,-11],['その他',null,null]];const crop=(name,x,y)=>x===null?`<span class="poll-card-icon poll-card-other" aria-hidden="true">？</span>`:`<span style="display:block;width:96px;height:96px;overflow:hidden;position:relative;border-radius:14px;box-shadow:0 1px 3px rgba(0,0,0,.18)"><img src="assets/card-sheet.webp" alt="${name}" style="position:absolute;width:831px;height:348px;max-width:none;left:${x}px;top:${y}px"></span>`;document.getElementById('content').innerHTML=`<h1>環境アンケート</h1><section id="poll" class="poll-section"><div class="poll-box"><div class="poll-title">絶対外せないのは？</div><div class="poll-sub">Ver.1.3.0</div><div class="poll-options">${pollOptions.map(([name,x,y])=>`<button type="button" class="poll-option" data-poll="${name}">${crop(name,x,y)}<span class="poll-option-label">${name}</span></button>`).join('')}</div><div id="pollMessage" class="poll-message">集計機能は準備中です。回答先を接続後、そのまま投票できるようにします。</div></div></section>`;const pollMessage=document.getElementById('pollMessage');document.querySelectorAll('.poll-option').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.poll-option').forEach(b=>b.classList.remove('selected'));btn.classList.add('selected');pollMessage.textContent=`「${btn.dataset.poll}」を選択中。集計先を接続後、このボタンから投票できます。`;localStorage.setItem('zooPollDraft',btn.dataset.poll)}));const draft=localStorage.getItem('zooPollDraft');if(draft){const b=[...document.querySelectorAll('.poll-option')].find(x=>x.dataset.poll===draft);if(b){b.classList.add('selected');pollMessage.textContent=`「${draft}」を選択中。集計先を接続後、このボタンから投票できます。`;}}})();